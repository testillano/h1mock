apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "h1mock-chart.fullname" . }}-test-connection"
  labels:
{{ include "h1mock-chart.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "h1mock-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
